<?php

// Class: IssuesCommentsManager_PDO.class.php
// Table: ts_issues_comments
// Generated by createEntity.php, written by Paulo Ferreira (paulo.ferreira@arkium.eu)
// Date : Sun, 23 Mar 2014 15:12:19 +0000
namespace Library\Models;

class IssuesCommentsManager_PDO extends IssuesCommentsManager {

	private function getData(\Library\HTTPRequest $request) {
		return new \Library\Entities\IssuesComments(array(
				'comment_id' => (int) $request->postData('comment_id'),
				'issue_id' => (int) $request->postData('issue_id'),
				'user_id' => (int) $request->postData('user_id'),
				'comment' => (string) $request->postData('comment'),
				'update_time' => (string) $request->postData('update_time'),
				'created_time' => (int) $request->postData('created_time') 
		));
	}

	public function getDatabases(\Library\HTTPRequest $request, \Library\Datatable $ini) {
		$table = new \Library\Datatables($request, $ini);
		return $table->run();
	}

	public function getList($debut = -1, $limite = -1) {
		$query = "SELECT * FROM ts_issues_comments";
		if ($debut != -1 || $limite != -1) {
			$query .= ' LIMIT ' . (int) $limite . ' OFFSET ' . (int) $debut;
		}
		try {
			$result = parent::$dao->query($query);
			$output = $result->fetchAll();
			$result->closeCursor();
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function getUnique($id) {
		$query = "SELECT 
			comment_id,
			issue_id,
			user_id,
			comment,
			update_time,
			created_time
		FROM ts_issues_comments
		WHERE comment_id = :id";
		try {
			$result = parent::$dao->prepare($query);
			$result->bindValue(':id', (int) $id, \PDO::PARAM_INT);
			$result->execute();
			// $result->setFetchMode(\PDO::FETCH_CLASS | \PDO::FETCH_PROPS_LATE, '\Library\Entities\IssuesComments');
			$output = (array) $result->fetch(\PDO::FETCH_ASSOC);
			
			$output['reponse'] = true;
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function count() {
		$query = "SELECT COUNT(*) FROM ts_issues_comments";
		return parent::$dao->query($query)->fetchColumn();
	}

	public function add(\Library\HTTPRequest $request) {
		$issuesComments = $this->getData($request);
		$query = "INSERT INTO ts_issues_comments SET 
			comment_id = :comment_id,
			issue_id = :issue_id,
			user_id = :user_id,
			comment = :comment,
			created_time = NOW()";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':comment_id', $issuesComments->comment_id());
			$result->bindValue(':issue_id', $issuesComments->issue_id());
			$result->bindValue(':user_id', $issuesComments->user_id());
			$result->bindValue(':comment', $issuesComments->comment());
			
			$output['reponse'] = $result->execute();
			$output['lastID'] = parent::$dao->lastInsertId();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function modify(\Library\HTTPRequest $request) {
		$issuesComments = $this->getData($request);
		$query = "UPDATE ts_issues_comments SET 
			comment_id = :comment_id,
			issue_id = :issue_id,
			user_id = :user_id,
			comment = :comment
		WHERE  = :id";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':id', $issuesComments->id(), \PDO::PARAM_INT);
			$result->bindValue(':comment_id', $issuesComments->comment_id());
			$result->bindValue(':issue_id', $issuesComments->issue_id());
			$result->bindValue(':user_id', $issuesComments->user_id());
			$result->bindValue(':comment', $issuesComments->comment());
			
			$output['reponse'] = $result->execute();
			$output['lastID'] = $issuesComments->id();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function delete($id) {
		$query = "DELETE FROM ts_issues_comments WHERE comment_id = " . (int) $id;
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			$output['reponse'] = $result->execute();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$output['reponse'] = 'The update of the database is not successful!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}
}