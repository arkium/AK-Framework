<?php

// Class: IssuesFilesManager_PDO.class.php
// Table: ts_issues_files
// Generated by createEntity.php, written by Paulo Ferreira (paulo.ferreira@arkium.eu)
// Date : Sat, 19 Apr 2014 13:33:15 +0000
namespace Library\Models;

class IssuesFilesManager_PDO extends IssuesFilesManager {

	private function getData(\Library\HTTPRequest $request) {
		return new \Library\Entities\IssuesFiles(array(
				'file_id' => (int) $request->postData('file_id'),
				'issue_id' => (int) $request->postData('issue_id'),
				'filename' => (string) $request->postData('filename'),
				'filesize' => (int) $request->postData('filesize'),
				'update_time' => (string) $request->postData('update_time'),
				'created_time' => (string) $request->postData('created_time') 
		));
	}

	public function getDatabases(\Library\HTTPRequest $request, \Library\Datatable $ini) {
		$table = new \Library\Datatables($request, $ini);
		return $table->run();
	}

	public function getList($where = null, $debut = -1, $limite = -1) {
		$query = "SELECT * FROM ts_issues_files";
		$query .= (!empty($where)) ? ' WHERE ' . (string) $where : '';
		$query .= ($debut != -1) ? ' LIMIT ' . (int) $limite : '';
		$query .= ($debut != -1 && $limite != -1) ? ' OFFSET ' . (int) $debut : '';
		try {
			$result = parent::$dao->query($query);
			$output = $result->fetchAll();
			$result->closeCursor();
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function getUnique($id) {
		$query = "SELECT 
			file_id,
			issue_id,
			filename,
			filesize,
			update_time,
			created_time
		FROM ts_issues_files
		WHERE file_id = :id";
		try {
			$result = parent::$dao->prepare($query);
			$result->bindValue(':id', (int) $id, \PDO::PARAM_INT);
			$result->execute();
			// $result->setFetchMode(\PDO::FETCH_CLASS | \PDO::FETCH_PROPS_LATE, '\Library\Entities\IssuesFiles');
			$output = (array) $result->fetch(\PDO::FETCH_NUM);
			
			$output['reponse'] = true;
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function count() {
		$query = "SELECT COUNT(*) FROM ts_issues_files";
		return parent::$dao->query($query)->fetchColumn();
	}

	public function add(\Library\HTTPRequest $request) {
		$issuesFiles = $this->getData($request);
		$query = "INSERT INTO ts_issues_files SET 
			issue_id = :issue_id,
			filename = :filename,
			filesize = :filesize,
			created_time = NOW()";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':issue_id', $issuesFiles->issue_id());
			$result->bindValue(':filename', $issuesFiles->filename());
			$result->bindValue(':filesize', $issuesFiles->filesize());
			
			$output['reponse'] = $result->execute();
			$this->lastInsertId = parent::$dao->lastInsertId();
			$output['lastID'] = $this->lastInsertId;
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function modify(\Library\HTTPRequest $request) {
		$issuesFiles = $this->getData($request);
		$query = "UPDATE ts_issues_files SET 
			issue_id = :issue_id,
			filename = :filename,
			filesize = :filesize
		WHERE file_id = :id";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':id', $issuesFiles->id(), \PDO::PARAM_INT);
			$result->bindValue(':issue_id', $issuesFiles->issue_id());
			$result->bindValue(':filename', $issuesFiles->filename());
			$result->bindValue(':filesize', $issuesFiles->filesize());
			
			$output['reponse'] = $result->execute();
			$output['lastID'] = $issuesFiles->id();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function delete($id) {
		$query = "DELETE FROM ts_issues_files WHERE file_id = " . (int) $id;
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			$output['reponse'] = $result->execute();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$output['reponse'] = 'The update of the database is not successful!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}
}