<?php

// Class: TasksTypesManager_PDO.class.php
// Table: ts_tasks_types
// Generated by createEntity.php, written by Paulo Ferreira (paulo.ferreira@arkium.eu)
// Date : Sun, 22 Dec 2013 13:39:33 +0000
namespace Library\Models;

class TasksTypesManager_PDO extends TasksTypesManager {

	private function getData(\Library\HTTPRequest $request) {
		return new \Library\Entities\TasksTypes(array(
				'task_type_id' => (int) $request->postData('task_type_id'),
				'code' => (string) $request->postData('code'),
				'name' => (string) $request->postData('name'),
				'task_family_id' => (int) $request->postData('task_family_id'),
				'chargeable' => (int) $request->postData('chargeable'),
				'note' => (string) $request->postData('note'),
				'color' => (string) $request->postData('color'),
				'status' => (int) $request->postData('status'),
				'update_time' => (string) $request->postData('update_time'),
				'created_time' => (string) $request->postData('created_time') 
		));
	}

	public function getDatabases(\Library\HTTPRequest $request, \Library\Datatable $ini) {
		$table = new \Library\Datatables($request, $ini);
		return $table->run();
	}

	public function getList($debut = -1, $limite = -1) {
		$query = "SELECT * FROM ts_tasks_types";
		if ($debut != -1 || $limite != -1) {
			$query .= ' LIMIT ' . (int) $limite . ' OFFSET ' . (int) $debut;
		}
		try {
			$result = parent::$dao->query($query);
			$output = $result->fetchAll();
			$result->closeCursor();
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function getUnique($id) {
		$query = "SELECT 
			task_type_id,
			code,
			name,
			task_family_id,
			chargeable,
			note,
			color,
			status,
			update_time,
			created_time
		FROM ts_tasks_types
		WHERE task_type_id = :id";
		try {
			$result = parent::$dao->prepare($query);
			$result->bindValue(':id', (int) $id, \PDO::PARAM_INT);
			$result->execute();
			// $result->setFetchMode(\PDO::FETCH_CLASS | \PDO::FETCH_PROPS_LATE, '\Library\Entities\TasksTypes');
			$output = (array) $result->fetch(\PDO::FETCH_NUM);
			
			$output['reponse'] = true;
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function count() {
		$query = "SELECT COUNT(*) FROM ts_tasks_types";
		return parent::$dao->query($query)->fetchColumn();
	}

	public function add(\Library\HTTPRequest $request) {
		$tasksTypes = $this->getData($request);
		$query = "INSERT INTO ts_tasks_types SET 
			code = :code,
			name = :name,
			task_family_id = :task_family_id,
			chargeable = :chargeable,
			note = :note,
			color = :color,
			status = :status,
			created_time = NOW()";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':code', $tasksTypes->code());
			$result->bindValue(':name', $tasksTypes->name());
			$result->bindValue(':task_family_id', $tasksTypes->task_family_id());
			$result->bindValue(':chargeable', $tasksTypes->chargeable());
			$result->bindValue(':note', $tasksTypes->note());
			$result->bindValue(':color', $tasksTypes->color());
			$result->bindValue(':status', $tasksTypes->status());
			
			$output['reponse'] = $result->execute();
			$this->lastInsertId = parent::$dao->lastInsertId();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function modify(\Library\HTTPRequest $request) {
		$tasksTypes = $this->getData($request);
		$query = "UPDATE ts_tasks_types SET 
			code = :code,
			name = :name,
			task_family_id = :task_family_id,
			chargeable = :chargeable,
			note = :note,
			color = :color,
			status = :status
		WHERE task_type_id = :id";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':id', $tasksTypes->id(), \PDO::PARAM_INT);
			$result->bindValue(':code', $tasksTypes->code());
			$result->bindValue(':name', $tasksTypes->name());
			$result->bindValue(':task_family_id', $tasksTypes->task_family_id());
			$result->bindValue(':chargeable', $tasksTypes->chargeable());
			$result->bindValue(':note', $tasksTypes->note());
			$result->bindValue(':color', $tasksTypes->color());
			$result->bindValue(':status', $tasksTypes->status());
			
			$output['reponse'] = $result->execute();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function delete($id) {
		$query = "DELETE FROM ts_tasks_types WHERE task_type_id = " . (int) $id;
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			$output['reponse'] = $result->execute();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$output['reponse'] = 'The update of the database is not successful!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}
}