<?php

// Class: TimesheetsManager_PDO.class.php
// Table: ts_timesheets
// Generated by createEntity.php, written by Paulo Ferreira (paulo.ferreira@arkium.eu)
// Date : Tue, 05 Nov 2013 19:05:42 +0000
namespace Library\Models;

class TimesheetsManager_PDO extends TimesheetsManager {

	private function getData(\Library\HTTPRequest $request) {
		return new \Library\Entities\Timesheets(array(
				'time_id' => (int) $request->postData('time_id'),
				'user_id' => (int) $request->postData('user_id'),
				'period_id' => (int) $request->postData('period_id'),
				'user_rate_id' => (int) $request->postData('user_rate_id'),
				'date' => (string) $request->postData('date'),
				'duration' => (string) $request->postData('duration'),
				'task_id' => (int) $request->postData('task_id'),
				'task_type_id' => (int) $request->postData('task_type_id'),
				'comment' => (string) $request->postData('comment'),
				'update_time' => (string) $request->postData('update_time'),
				'created_time' => (string) $request->postData('created_time') 
		));
	}

	public function getDatabases(\Library\HTTPRequest $request, \Library\Datatable $ini) {
		$table = new \Library\Datatables($request, $ini);
		return $table->run();
	}

	public function getList($debut = -1, $limite = -1) {
		$query = "SELECT * FROM ts_timesheets";
		if ($debut != -1 || $limite != -1) {
			$query .= ' LIMIT ' . (int) $limite . ' OFFSET ' . (int) $debut;
		}
		try {
			$result = parent::$dao->query($query);
			$output = $result->fetchAll();
			$result->closeCursor();
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function getUnique($id) {
		$query = "SELECT 
			time_id,
			user_id,
			period_id,
			user_rate_id,
			date,
			duration,
			task_id,
			task_type_id,
			comment,
			update_time,
			created_time
		FROM ts_timesheets
		WHERE time_id = :id";
		try {
			$result = parent::$dao->prepare($query);
			$result->bindValue(':id', (int) $id, \PDO::PARAM_INT);
			$result->execute();
			// $result->setFetchMode(\PDO::FETCH_CLASS | \PDO::FETCH_PROPS_LATE, '\Library\Entities\Timesheets');
			$output = (array) $result->fetch(\PDO::FETCH_NUM);
			
			$output['reponse'] = true;
		} catch (\PDOException $e) {
			$output['reponse'] = 'The database is not able to be read!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}

	public function count() {
		$query = "SELECT COUNT(*) FROM ts_timesheets";
		return parent::$dao->query($query)->fetchColumn();
	}

	public function add(\Library\HTTPRequest $request) {
		$timesheets = $this->getData($request);
		$query = "INSERT INTO ts_timesheets SET 
			user_id = :user_id,
			period_id = :period_id,
			user_rate_id = :user_rate_id,
			date = :date,
			duration = :duration,
			task_id = :task_id,
			task_type_id = :task_type_id,
			comment = :comment,
			created_time = NOW()";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':user_id', $timesheets->user_id());
			$result->bindValue(':period_id', $timesheets->period_id());
			$result->bindValue(':user_rate_id', $timesheets->user_rate_id());
			$result->bindValue(':date', $timesheets->date());
			$result->bindValue(':duration', $timesheets->duration());
			$result->bindValue(':task_id', $timesheets->task_id());
			$result->bindValue(':task_type_id', $timesheets->task_type_id());
			$result->bindValue(':comment', $timesheets->comment());
			
			$output['reponse'] = $result->execute();
			$output['lastID'] = parent::$dao->lastInsertId();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function modify(\Library\HTTPRequest $request) {
		$timesheets = $this->getData($request);
		$query = "UPDATE ts_timesheets SET 
			user_id = :user_id,
			period_id = :period_id,
			user_rate_id = :user_rate_id,
			date = :date,
			duration = :duration,
			task_id = :task_id,
			task_type_id = :task_type_id,
			comment = :comment
		WHERE time_id = :id";
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			
			$result->bindValue(':id', $timesheets->id(), \PDO::PARAM_INT);
			$result->bindValue(':user_id', $timesheets->user_id());
			$result->bindValue(':period_id', $timesheets->period_id());
			$result->bindValue(':user_rate_id', $timesheets->user_rate_id());
			$result->bindValue(':date', $timesheets->date());
			$result->bindValue(':duration', $timesheets->duration());
			$result->bindValue(':task_id', $timesheets->task_id());
			$result->bindValue(':task_type_id', $timesheets->task_type_id());
			$result->bindValue(':comment', $timesheets->comment());
			
			$output['reponse'] = $result->execute();
			$output['lastID'] = $timesheets->id();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$error = $result->errorInfo();
			if ($error[1] == '1062') {
				$output['reponse'] = 'This code already exists in the database.';
			} else {
				$output['reponse'] = 'The update of the database is not successful!<br/>';
				$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
				$output['reponse'] .= "Syntax Error: " . $e->getMessage();
			}
		}
		return $output;
	}

	public function delete($id) {
		$query = "DELETE FROM ts_timesheets WHERE time_id = " . (int) $id;
		try {
			parent::$dao->beginTransaction();
			$result = parent::$dao->prepare($query);
			$output['reponse'] = $result->execute();
			parent::$dao->commit();
		} catch (\PDOException $e) {
			parent::$dao->rollback();
			$output['reponse'] = 'The update of the database is not successful!<br/>';
			$output['reponse'] .= "Code Error: " . $e->getCode() . "<br/>";
			$output['reponse'] .= "Syntax Error: " . $e->getMessage();
		}
		return $output;
	}
}